.closeButton {
  background-color: #ffffff;
  position: absolute;
  top: 5px;
  left: 5px;
  border: none;
}

.modal {
  margin-top: 10px;
}

.modalWrapper {
  min-height: 250px;
  display: flex;
  flex-direction: column;
  justify-content: space-between
}

.footer {
  align-self: center;
}

.button {
  background-color: #ffffff;
  border: none;
}

.buttonWrapper {
  margin: auto;
}