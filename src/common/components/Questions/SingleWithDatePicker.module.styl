.title {
    composes: title from './Questions.module.styl';
}

.row {
    composes row from './Questions.module.styl'
}

.inputWrapper {
    margin-top: 10px;
    margin-bottom: 10px;
}

.datePickerWrapper {
    z-index: 10001;
    position: relative;
}