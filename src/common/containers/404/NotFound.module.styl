@import '../../styles/variables.styl'

.container {
    background-color: #1e87f0;
    width: 100%;
    height: 100%;
    padding-top 65px;
    display: flex
    flex-direction column
    align-items center
    justify-content center
    min-height 500px
    @media (max-width $md) {
        min-height 250px
    }
}

.header {
    color: #fff;
    text-align: center;
    font-size 100px
}

.text {
    color: #ffffff;
    text-align: center;
    font-size: 40px
}